import std::io;
import std::collections::list;
import std::encoding::json;

fn void main(){
    List{int} list ;
    io::printn("hello");
    list.init(mem);
    io::printn("hello");
    io::printf("s");
}
fn int add(){
    return 0;
}
